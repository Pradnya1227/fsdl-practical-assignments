:root{
  --lemon: #d9ff3a;
  --mint: #bff7c7;
  --mint2:#a9f2b6;
  --bg: #f7f8f3;
  --offwhite: #f3f1e8;
  --ink: #1c231b;
  --muted:#5b6559;
  --border: rgba(243,241,232,0.9);
  --glass: rgba(255,255,255,0.35);
  --shadow: 0 12px 30px rgba(17, 22, 16, 0.18);
  --radius: 22px;
}

*{ box-sizing:border-box; }
html,body{ height:100%; }
body{
  font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
  color: var(--ink);
  background:
    radial-gradient(900px 600px at 15% 10%, rgba(217,255,58,0.25), transparent 60%),
    radial-gradient(900px 600px at 85% 20%, rgba(169,242,182,0.28), transparent 60%),
    linear-gradient(180deg, #eef7ea 0%, var(--bg) 55%, #edf6ea 100%);
}

/* subtle palace pattern */
body::before{
  content:"";
  position:fixed;
  inset:0;
  pointer-events:none;
  opacity:0.22;
  background:
    radial-gradient(circle at 1px 1px, rgba(28,35,27,0.06) 1px, transparent 2px);
  background-size: 22px 22px;
  mix-blend-mode:multiply;
}

.letterwide{ letter-spacing: .08em; }
.muted{ color: var(--muted); }

/* Top strip */
.top-marquee{
  background: linear-gradient(90deg, rgba(217,255,58,0.35), rgba(191,247,199,0.35));
  border-bottom: 1px solid rgba(255,255,255,0.55);
}

/* Nav */
.palace-nav{
  background: rgba(247,248,243,0.75);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid rgba(255,255,255,0.55);
}
.navbar-brand .brand-name{
  font-family: Cinzel, serif;
  font-weight: 700;
  letter-spacing: .03em;
}
.brand-crest{
  width: 38px; height: 38px;
  display:grid; place-items:center;
  border-radius: 12px;
  background: linear-gradient(180deg, rgba(217,255,58,0.45), rgba(191,247,199,0.4));
  border: 1px solid rgba(255,255,255,0.7);
  box-shadow: 0 10px 25px rgba(28,35,27,0.12);
  font-weight: 800;
}

.nav-link{
  color: var(--ink);
  opacity: .86;
}
.nav-link:hover{ opacity:1; }

.btn-ghost{
  border: 1px solid rgba(243,241,232,0.95);
  background: rgba(255,255,255,0.35);
  backdrop-filter: blur(10px);
  border-radius: 14px;
  padding: 10px 14px;
}
.btn-ghost:hover{
  background: rgba(255,255,255,0.55);
}

.btn-gold{
  border: 1px solid rgba(243,241,232,0.95);
  background: linear-gradient(180deg, rgba(217,255,58,0.55), rgba(191,247,199,0.5));
  border-radius: 14px;
  padding: 10px 14px;
  box-shadow: 0 14px 30px rgba(28,35,27,0.14);
}
.btn-gold:hover{
  filter: brightness(1.02);
}

.badge-soft{
  display:inline-grid; place-items:center;
  min-width: 26px;
  height: 24px;
  padding: 0 8px;
  border-radius: 999px;
  background: rgba(255,255,255,0.55);
  border: 1px solid rgba(243,241,232,0.95);
  font-size: 12px;
  margin-left: 6px;
}

/* Hero */
.hero{ padding: 56px 0 24px; }
.hero-frame{
  position:relative;
  padding: 34px;
  border-radius: calc(var(--radius) + 10px);
  background: rgba(255,255,255,0.28);
  border: 1px solid rgba(243,241,232,0.9);
  box-shadow: var(--shadow);
  overflow:hidden;
}
.hero-border{
  position:absolute;
  inset: 10px;
  border-radius: calc(var(--radius) + 4px);
  border: 1px solid rgba(243,241,232,0.9);
  pointer-events:none;
}

.hero-kicker{
  text-transform: uppercase;
  letter-spacing: .14em;
  font-size: 12px;
  color: var(--muted);
}
.hero-title{
  font-family: Cinzel, serif;
  font-weight: 700;
  font-size: clamp(2rem, 3.5vw, 3.1rem);
  margin: 10px 0 12px;
}
.highlight{
  background: linear-gradient(90deg, rgba(217,255,58,0.55), rgba(191,247,199,0.45));
  padding: 0 8px;
  border-radius: 10px;
  border: 1px solid rgba(243,241,232,0.9);
}
.hero-sub{
  color: var(--muted);
  max-width: 52ch;
}

.hero-stats{
  display:flex;
  gap: 12px;
}
.stat{
  flex:1;
  padding: 12px 14px;
  border-radius: 18px;
  background: rgba(255,255,255,0.25);
  border: 1px solid rgba(243,241,232,0.9);
}
.stat-num{
  font-family: Cinzel, serif;
  font-weight: 700;
  font-size: 20px;
}
.stat-label{
  font-size: 12px;
  color: var(--muted);
  letter-spacing: .08em;
  text-transform: uppercase;
}

/* Showcase */
.hero-showcase{
  position:relative;
  min-height: 360px;
  border-radius: var(--radius);
  background:
    radial-gradient(500px 300px at 30% 30%, rgba(217,255,58,0.22), transparent 60%),
    radial-gradient(500px 300px at 70% 25%, rgba(169,242,182,0.24), transparent 60%),
    linear-gradient(180deg, rgba(255,255,255,0.25), rgba(255,255,255,0.15));
  border: 1px solid rgba(243,241,232,0.9);
  overflow:hidden;
}

.arch{
  position:absolute;
  width: 260px;
  height: 420px;
  top: -40px;
  border-radius: 260px 260px 30px 30px;
  background: rgba(255,255,255,0.15);
  border: 1px solid rgba(243,241,232,0.9);
  backdrop-filter: blur(10px);
}
.arch-left{ left: -70px; }
.arch-right{ right: -70px; }

.bottle{
  position:absolute;
  bottom: 20px;
  width: 160px;
  height: 250px;
  border-radius: 26px;
  border: 1px solid rgba(243,241,232,0.92);
  background: linear-gradient(180deg, rgba(255,255,255,0.35), rgba(255,255,255,0.15));
  box-shadow: 0 18px 40px rgba(28,35,27,0.18);
}
.bottle .cap{
  position:absolute;
  top: -24px;
  left: 50%;
  transform: translateX(-50%);
  width: 62px;
  height: 40px;
  border-radius: 14px;
  background: linear-gradient(180deg, rgba(217,255,58,0.65), rgba(191,247,199,0.55));
  border: 1px solid rgba(243,241,232,0.95);
}
.bottle .label{
  position:absolute;
  left: 14px;
  right: 14px;
  bottom: 14px;
  padding: 12px;
  border-radius: 18px;
  background: rgba(247,248,243,0.55);
  border: 1px solid rgba(243,241,232,0.95);
  backdrop-filter: blur(10px);
}
.label-title{
  font-family: Cinzel, serif;
  font-weight: 700;
}
.label-sub{
  font-size: 12px;
  color: var(--muted);
  letter-spacing: .06em;
  text-transform: uppercase;
}

.bottle-1{ left: 14%; transform: rotate(-4deg); }
.bottle-2{ right: 12%; transform: rotate(4deg); height: 265px; }

.floating-card{
  position:absolute;
  top: 18px;
  left: 18px;
  padding: 12px 14px;
  border-radius: 18px;
  background: rgba(255,255,255,0.3);
  border: 1px solid rgba(243,241,232,0.9);
  backdrop-filter: blur(12px);
  box-shadow: 0 12px 28px rgba(28,35,27,0.12);
}
.deal-line{
  font-family: Cinzel, serif;
  font-weight: 700;
  font-size: 18px;
}

/* Sections */
.section{ padding: 44px 0; }
.section-alt{
  background: rgba(255,255,255,0.18);
  border-top: 1px solid rgba(243,241,232,0.7);
  border-bottom: 1px solid rgba(243,241,232,0.7);
}
.section-head{
  display:flex;
  justify-content: space-between;
  align-items: center;
  gap: 18px;
  margin-bottom: 16px;
}
.section-title{
  font-family: Cinzel, serif;
  font-weight: 700;
  margin: 0;
}
.section-sub{
  margin: 6px 0 0;
  color: var(--muted);
}

/* Cards */
.palace-card{
  height: 100%;
  border-radius: var(--radius);
  padding: 18px;
  background: rgba(255,255,255,0.26);
  border: 1px solid rgba(243,241,232,0.92);
  box-shadow: 0 12px 26px rgba(28,35,27,0.10);
  position:relative;
  overflow:hidden;
}
.palace-card::after{
  content:"";
  position:absolute;
  inset:-40px -60px auto auto;
  width: 140px;
  height: 140px;
  border-radius: 999px;
  background: radial-gradient(circle at 30% 30%, rgba(217,255,58,0.35), transparent 60%);
}
.card-ornament{
  width: 44px; height:44px;
  display:grid; place-items:center;
  border-radius: 16px;
  background: rgba(255,255,255,0.35);
  border: 1px solid rgba(243,241,232,0.92);
  margin-bottom: 10px;
}
.card-title{ font-weight: 700; margin: 0 0 6px; }
.card-text{ color: var(--muted); margin: 0 0 10px; }
.card-link{
  text-decoration:none;
  color: var(--ink);
  font-weight: 600;
  border-bottom: 1px solid rgba(28,35,27,0.3);
}
.card-link:hover{ opacity: .9; }

/* Filters */
.filters{
  display:flex;
  gap: 10px;
  align-items:center;
  flex-wrap: wrap;
}
.palace-input .input-group-text,
.palace-input .form-select{
  border: 1px solid rgba(243,241,232,0.92);
  background: rgba(255,255,255,0.35);
}
.btn-filter{
  border: 1px solid rgba(243,241,232,0.92) !important;
  background: rgba(255,255,255,0.28) !important;
}
.btn-filter.active{
  background: linear-gradient(180deg, rgba(217,255,58,0.5), rgba(191,247,199,0.45)) !important;
}

/* Product cards */
.product-card{
  height:100%;
  border-radius: var(--radius);
  border: 1px solid rgba(243,241,232,0.92);
  background: rgba(255,255,255,0.26);
  box-shadow: 0 12px 26px rgba(28,35,27,0.10);
  overflow:hidden;
}
.product-media{
  height: 170px;
  position:relative;
  border-bottom: 1px solid rgba(243,241,232,0.9);
  background:
    radial-gradient(180px 120px at 35% 35%, rgba(217,255,58,0.35), transparent 65%),
    radial-gradient(180px 120px at 70% 30%, rgba(169,242,182,0.35), transparent 65%),
    linear-gradient(180deg, rgba(255,255,255,0.25), rgba(255,255,255,0.10));
}
.product-pill{
  position:absolute;
  top: 12px;
  left: 12px;
  font-size: 12px;
  padding: 6px 10px;
  border-radius: 999px;
  background: rgba(255,255,255,0.45);
  border: 1px solid rgba(243,241,232,0.92);
  backdrop-filter: blur(10px);
}
.product-body{ padding: 14px; }
.product-name{
  font-family: Cinzel, serif;
  font-weight: 700;
  margin: 0 0 6px;
}
.product-notes{
  color: var(--muted);
  font-size: 13px;
  margin: 0 0 10px;
}
.product-meta{
  display:flex;
  justify-content: space-between;
  align-items:center;
  gap: 10px;
}
.price{
  font-weight: 700;
}
.rating{
  font-size: 13px;
  color: var(--muted);
  white-space: nowrap;
}
.product-actions{
  display:flex;
  gap: 8px;
  margin-top: 12px;
}

/* Panels */
.palace-panel{
  border-radius: calc(var(--radius) + 4px);
  padding: 22px;
  background: rgba(255,255,255,0.24);
  border: 1px solid rgba(243,241,232,0.92);
  box-shadow: 0 12px 26px rgba(28,35,27,0.10);
}

.palace-list{
  list-style:none;
  padding:0;
  margin:0;
  color: var(--muted);
}
.palace-list li{
  display:flex;
  align-items:flex-start;
  gap: 10px;
  padding: 6px 0;
}
.dot{
  width: 10px;
  height: 10px;
  border-radius: 999px;
  background: linear-gradient(180deg, rgba(217,255,58,0.65), rgba(191,247,199,0.55));
  border: 1px solid rgba(243,241,232,0.92);
  margin-top: 6px;
}

/* Gallery tiles */
.gallery-grid{
  display:grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
}
.gallery-tile{
  border-radius: 22px;
  border: 1px solid rgba(243,241,232,0.92);
  background:
    radial-gradient(220px 160px at 30% 30%, rgba(217,255,58,0.35), transparent 65%),
    radial-gradient(220px 160px at 70% 30%, rgba(169,242,182,0.32), transparent 65%),
    linear-gradient(180deg, rgba(255,255,255,0.25), rgba(255,255,255,0.08));
  min-height: 140px;
  box-shadow: 0 12px 26px rgba(28,35,27,0.10);
}
.tile-1{ min-height: 170px; }
.tile-4{ min-height: 170px; }

/* Forms */
.palace-form .form-control{
  border-radius: 14px;
  border: 1px solid rgba(243,241,232,0.92);
  background: rgba(255,255,255,0.32);
}
.palace-form .form-control:focus{
  box-shadow: none;
  border-color: rgba(217,255,58,0.55);
}

/* Modal / offcanvas */
.palace-modal, .palace-canvas{
  background: rgba(247,248,243,0.78);
  backdrop-filter: blur(14px);
  border: 1px solid rgba(243,241,232,0.92);
}
.cart-item{
  display:flex;
  gap: 10px;
  padding: 12px;
  border-radius: 18px;
  background: rgba(255,255,255,0.28);
  border: 1px solid rgba(243,241,232,0.92);
}
.cart-thumb{
  width: 54px;
  height: 54px;
  border-radius: 16px;
  border: 1px solid rgba(243,241,232,0.92);
  background:
    radial-gradient(80px 60px at 35% 35%, rgba(217,255,58,0.35), transparent 70%),
    radial-gradient(80px 60px at 70% 30%, rgba(169,242,182,0.32), transparent 70%),
    linear-gradient(180deg, rgba(255,255,255,0.25), rgba(255,255,255,0.08));
}
.cart-summary{
  border-radius: 18px;
  padding: 14px;
  background: rgba(255,255,255,0.28);
  border: 1px solid rgba(243,241,232,0.92);
}
.cart-summary .total{
  font-weight: 800;
  font-size: 18px;
}

.footer{
  border-top: 1px solid rgba(243,241,232,0.7);
  background: rgba(255,255,255,0.15);
}
.footer-link{
  color: var(--muted);
  text-decoration: none;
}
.footer-link:hover{ color: var(--ink); }

/* Small screens */
@media (max-width: 576px){
  .hero-frame{ padding: 22px; }
  .hero-showcase{ min-height: 320px; }
  .bottle{ width: 145px; height: 235px; }
  .bottle-1{ left: 8%; }
  .bottle-2{ right: 6%; }
}